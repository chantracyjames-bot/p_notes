
# Alias (alias)
- Definition:
	- Used to create shortcut names or nicknames for other commands.
- Syntax:
```
alias <alias> <command>
```
- Example:
```
alias gc'git commit'
```
- Note:
	- Aliases are commonly written to the file "~/.bashrc" or "~/.bash_profile", as it makes the alias permanent.